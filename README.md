#TDD Practice

Capitalize function - capitalize the first character of a string

reverseString - a function that takes a string and reverses it

Calculator - calculator with basic operations + - / \*

caeserCipher - a function that takes a string and a shift factor which returns it with each character "shifted"
